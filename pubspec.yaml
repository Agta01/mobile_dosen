name: mobile_dosen
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  fl_chart: ^0.69.2
  shared_preferences: ^2.0.15
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  assets:
    - assets/jti_logo.png
    # - assets/poli.jpg

  uses-material-design: true
